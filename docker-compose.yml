# Phase 11 self-host target — NOT active in Phase 1.
# This file documents the intended topology only. Do not depend on it yet.
# Phase 1 runs with zero infra: `npm install && npm run dev`.

name: tuberack-phase11-target

services:
  # postgres:
  #   image: postgres:16
  #   environment:
  #     POSTGRES_DB: tuberack
  #     POSTGRES_USER: tuberack
  #     POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-change-me}
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #   ports: ["5432:5432"]

  # redis:
  #   image: redis:7
  #   ports: ["6379:6379"]

  # minio:
  #   image: minio/minio:latest
  #   command: server /data --console-address ":9001"
  #   volumes:
  #     - miniodata:/data
  #   ports: ["9000:9000", "9001:9001"]

# volumes:
#   pgdata:
#   miniodata:
